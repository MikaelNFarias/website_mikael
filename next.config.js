/** @type {import('next').NextConfig} */
const nextConfig = {
    compiler: {
      styledComponents: true, // Ativa suporte nativo ao styled-components
    },
    reactStrictMode: true,
  };
  
  module.exports = nextConfig;
  